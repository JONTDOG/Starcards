from __future__ import annotations

from pathlib import Path


def sanitize_name(name: str) -> str:
    pass


def deck_name_for_section(root_deck: str, ancestors, title: str, card_type: str) -> str:
    pass


def build_output_path(output_dir: Path, ancestors, title: str, suffix: str) -> Path:
    pass
