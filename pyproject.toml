[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "starcards-public"
version = "0.1.0"
description = "Public architectural scaffold for the StarCards PDF-to-Anki platform."
requires-python = ">=3.11"
